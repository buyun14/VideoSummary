# 第一阶段实施建议（Backbone）

## 目标
在成本可控前提下，把原始视频转成高质量、带时间戳的文本骨架，为后续视觉按需理解提供索引。

## 推荐实现架构

1. Ingest 层
- 输入：本地视频文件路径
- 责任：文件校验、任务 ID 生成、输出目录规划

2. Media 层
- 技术：`ffmpeg`
- 责任：提取 16k/mono wav（统一 ASR 输入，降低噪声与算力波动）

3. ASR 层
- 技术：`faster-whisper`
- 责任：
  - 自动或指定语言识别
  - 片段时间戳 + 词级时间戳
  - 开启 VAD 过滤静音段

4. Artifact 层
- 输出：`json` / `jsonl` / `txt` / `srt` / `manifest`
- 责任：
  - 既能人看（txt/srt）
  - 也能机器消费（json/jsonl）
  - manifest 用于流水线追踪

## 技术栈建议

- 语言：Python 3.10+
- 音频处理：ffmpeg CLI（稳定、跨平台、无额外 binding 复杂度）
- ASR：faster-whisper
- 数据格式：JSON/JSONL/SRT
- 工程形态：CLI 优先（先可跑，再服务化）

## 关键参数建议

- `model`：
  - 试验期：`small`
  - 低成本批量：`base` 或 `small`
  - 高精度：`medium` / `large-v3`
- `compute_type`：
  - CPU：`int8`
  - GPU：`float16` 或 `int8_float16`
- `language`：中文场景建议显式 `zh`，减少误检

## 成本与性能控制

1. 预处理统一音频规格（16k mono）减少 ASR 抖动
2. 保留中间 wav，失败可重试 ASR，不必重复解码视频
3. 按视频时长分批执行，先做抽样评估再扩到全量

## 质量评估指标（建议你马上加）

- 识别覆盖率：有文字时间占比
- 平均片段长度：过长说明分段策略需调
- 专有名词准确率：手工抽检 TopN 关键词
- 可读性得分：标点恢复后主观打分

## 从第一阶段到第二阶段的接口约定

建议第二阶段只读取 `transcript.segments.jsonl`，字段固定：
- `start`
- `end`
- `text`
- `words`

这样可以把“路由器”做成独立模块，不耦合 ASR 细节。
