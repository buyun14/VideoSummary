# 第二阶段路由设计（Router）

## 目标
从第一阶段转录结果中筛选“需要视觉理解”的时间点，生成可执行的关键帧计划，避免全量 VLM 成本。

## 输入
- `transcript.segments.jsonl`（来自 phase1）

每条记录最少字段：
- `start`
- `end`
- `text`

## 输出
- `router.scored_segments.jsonl`：每段文本分数与理由
- `keyframe_plan.json`：关键帧计划（给截图/VLM）
- `keyframe_plan.md`：人工审阅版
- `phase2_router_manifest.json`：运行统计

## 当前路由策略（可解释、低成本）

1. 规则打分
- 视觉提示词（如“如图”“表格”“代码”“as shown”）
- 数据密度（数字数量）
- 指代不清（“这个/这里/this/that”）
- 结构化讲解信号（“第一部分/第二部分/总结”等）

2. 候选选择
- 保留 `score >= min_score`
- 按 `top_k` 截断
- 邻近时间窗合并（`merge_gap_seconds`）

3. 回退补采样
- 若命中不足，按时间轴均匀补齐到 `min_plan_items`
- 避免 ASR 噪声导致候选过少

## 为什么这样做
- 成本低：无需额外调用大模型即可先筛选
- 可解释：每个候选有 `reasons`
- 可控：参数可调（阈值、数量、窗口）

## 建议默认参数（中文口播视频）
- `top_k=18`
- `min_score=2.0`
- `merge_gap_seconds=8`
- `context_window=1`
- `min_plan_items=8`

## 与第三阶段接口建议

`keyframe_plan.json` 的每项可直接驱动截图任务：
- `capture_time`
- `window_start`
- `window_end`
- `query_intent`
- `context`

第三阶段建议流程：
1. 按 `capture_time` 截图（可取前后帧）
2. 将截图 + `query_intent` + `context` 发送给 VLM
3. 输出结构化视觉补全（图表、代码、页面元素）
