# 第二阶段固定采样框架（默认基线）

## 设计目标
先建立一个稳定、可复现、可对比实验的基础框架：
- 固定时间戳采样
- 固定参数截图
- 固定载荷结构
- 不做意图识别/路由策略

## 输入
- `phase1` 目录（必须包含）：
  - `phase1_manifest.json`
  - `transcript.full.json`
  - `transcript.segments.jsonl`

## 输出
- `fixed_keyframe_plan.json`：固定采样计划
- `vlm_payload.jsonl`：视觉模型输入载荷
- `screenshots/`：截图文件
- `phase2_fixed_manifest.json`：统计与配置
- `audio_summary.txt`：可选

## 核心固定逻辑
1. 按固定间隔生成时间戳（如每 30 秒）
2. 按时间戳截屏
3. 从 phase1 转录中找到该时间附近分片文本
4. 组装 VLM 载荷（带时间戳、相对位置、文本辅助信息）

## 可选开关（用于对比）
- `--without-segment-text`
- `--context-window N`
- `--with-audio-summary`
- `--no-capture-images`

建议做 A/B 对比：
- A: 只用截图 + 时间戳
- B: A + 分片文本
- C: B + 上下文窗口
- D: C + 音频摘要

## 为什么先做这个
- 能快速验证“截图+文本辅助”是否真的提升
- 避免过早绑定特定视频类型规则
- 为后续 router/智能体方案提供统一基线
